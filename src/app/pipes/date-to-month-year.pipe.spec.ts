import { DateToMonthYearPipe } from './date-to-month-year.pipe';

describe('DateToMonthYearPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToMonthYearPipe();
    expect(pipe).toBeTruthy();
  });
});
